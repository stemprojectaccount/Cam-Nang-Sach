rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /readable_books/{bookId} {
      allow read, write: if true;
    }
    
    match /suggested_books/{bookId} {
      allow read, write: if true;
    }
  }
}
